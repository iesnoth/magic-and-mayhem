import React, { Fragment } from "react";


const EditUser = () => {
    return (<Fragment>

  {/* How to make a modal in CSS? Do we need to use a library? Material UI? */}
    </Fragment>)
};

export default EditUser;